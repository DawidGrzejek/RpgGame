# RPG Game Environment Configuration
# Copy this file to .env or set these environment variables

# Database Connection Strings
# Set your actual database connection string here
CONNECTION_STRING_DEFAULT=Server=your-server; Database=your-database; User Id=your-user; Password=your-password; Encrypt=True; TrustServerCertificate=True; MultipleActiveResultSets=True;

# Seed User Configuration  
SEED_USER_PASSWORD=YourSecurePassword123!

# Usage:
# 1. Copy this file to .env
# 2. Update the values with your actual credentials
# 3. The .env file is gitignored and won't be committed
# 4. If not set, application will use fallback values from appsettings.Development.json